import { View, Text, ActivityIndicator } from 'react-native';
import styled from "styled-components";

const LoadingWrapper = styled(View)``;

const AnimatedIcon = styled(ActivityIndicator)``;

const LoadingTitle = styled(Text)``;

const LoadingDescription = styled(Text)``;

export default {
  LoadingWrapper, AnimatedIcon, LoadingTitle, LoadingDescription
};
